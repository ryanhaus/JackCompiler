#pragma once
#include <fstream>
#include <iostream>
#include <string>

/**
 * \brief An instance of a Jack program
 */
class jack_program
{
public:
	/**
	 * \brief Creates an instance of a Jack program from a file
	 * \param file_name Name of the file to read the program from
	 */
	jack_program(const char* file_name);
private:
};